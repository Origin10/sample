//output:../../views/static_pages/home.blade.php

@extends('layouts.default')
@section('content')
@if (Auth::check())
.row
  .col-md-8
    section.status_form
    @include('shared._status_form')
    h3 微博列表
    @include('shared._feed')
  aside.col-md-4
    section.user_info
      @include('shared._user_info', ['user' => Auth::user()])
    section.stats
      @include('shared._stats',['user' => Auth::user()])
@else
.jumbotron
  h1 Hello Laravel
  p.lead
    你現在所看的是
    a(href='https://laravel-china.org/laravel-tutorial/5.1') Laravel 入門教程
    span 的項目主頁。
  p 一切將從這裡開始。
  p
    a.btn.btn-lg.btn-success(href="{{ route('signup') }}", role='button') 現在註冊
@endif
@stop
